export enum UnitsSystem {
  metric = "metric",
  imperial = "imperial",
  standard = "standard",
}

export enum TemperatureUnitsSymbols {
  metric = "°C",
  imperial = "°F",
  standard = "K",
}

export enum PressureUnitsSymbols {
  metric = "hPa",
  imperial = "inHg",
  standard = "mb",
}

export enum SpeedUnitsSymbols {
  metric = "m/s",
  imperial = "mph",
  standard = "km/h",
}

export enum HumidityUnitsSymbols {
  metric = "%",
  imperial = "",
  standard = "",
}
